export interface BusinessPlan {
  createdAt?: string;
  description?: string;
  id?: string;
  isDone?: boolean;
  revisions?: number;
  section?: number;
  title: string;
  updatedAt?: string;
  userId?: string;
}
