export interface BusinessPlan {
  createdAt?: string;
  description?: string;
  uid?: string;
  isDone?: boolean;
  availableRevisions?: number;
  section?: number;
  title: string;
  updatedAt?: string;
  userId?: string;
}
